# GitHub Pages Reference Mode

This bundle contains the complete client-gated static reference application and can be deployed directly to GitHub Pages. The wallet gate rechecks current direct eligibility before opening the interface, but the underlying static files are publicly downloadable by construction.

For genuine exclusive delivery, use `Successor_Omega_x_AGI_Jobs_v3_0_0_PROTECTED_DEPLOYMENT.zip`, which publishes only the access shell and serves the application through a server-side gateway and secret private origin.
