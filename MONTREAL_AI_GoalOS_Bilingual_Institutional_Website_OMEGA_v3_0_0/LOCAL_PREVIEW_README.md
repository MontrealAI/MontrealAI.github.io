# Local preview

1. Open `START_HERE.html` directly; all links use explicit `index.html` files.
2. For an HTTP preview, run `START_LOCAL_PREVIEW.command` on macOS or `START_LOCAL_PREVIEW.bat` on Windows.
3. Do not enter confidential or sensitive information into any public page.
