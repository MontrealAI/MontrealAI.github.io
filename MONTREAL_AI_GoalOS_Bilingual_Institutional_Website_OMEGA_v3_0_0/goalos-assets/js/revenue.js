document.addEventListener('DOMContentLoaded',()=>{const $=id=>document.getElementById(id);const fmt=n=>new Intl.NumberFormat('en-CA',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n||0);const form=$('readiness-form');if(!form)return;form.addEventListener('submit',e=>{e.preventDefault();const ok=$('safe-input').checked;if(!ok){alert(document.documentElement.lang.startsWith('fr')?'Confirmez que vous n’avez saisi aucun renseignement confidentiel.':'Confirm that you entered no confidential information.');return;}const value=+$('value-pool').value||0,cost=+$('variable-cost').value||0,gm=(+$('margin-floor').value||75)/100,days=+$('deadline').value||0,sponsor=$('sponsor').value,budget=$('budget').value,risk=$('risk-class').value;const floor=gm<1?cost/(1-gm):0;let offer='Proof Readiness Scan';let price=0;if(value>=1000000&&budget==='ready'){offer='Standard GoalOS Proof Mission';price=Math.max(45000,floor)}else if(value>=180000){offer='Proof Mission Activation';price=Math.max(15000,floor)}else if(value>=100000){offer='Private Frontier MasterClass';price=Math.max(25000,floor)}else{offer='Decline / narrow the objective';price=0;}const ratio=price?value/price:0;const admission= sponsor==='yes'&&budget!=='none'&&days>0&&ratio>=4&&risk!=='prohibited';$('r-offer').textContent=offer;$('r-price').textContent=price?fmt(Math.ceil(price/1000)*1000):'—';$('r-ratio').textContent=price?ratio.toFixed(1)+'×':'—';$('r-state').textContent=admission?'ADMIT / PROCEED':'CONDITION / NARROW';$('result-box').classList.add('show');const payload={generated_at:new Date().toISOString(),public_non_confidential:true,offer,recommended_price:price,value_pool:value,value_to_fee_ratio:ratio,admission_state:admission?'ADMIT':'CONDITION',inputs:{sponsor,budget,deadline_days:days,risk_class:risk,variable_cost:cost,margin_floor:gm}};$('export-json').onclick=()=>{const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='GoalOS_Public_Readiness_Scan.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);};});});