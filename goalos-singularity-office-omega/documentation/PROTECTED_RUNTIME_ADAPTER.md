# Protected Runtime Adapter

The static Office contains a deterministic local institutional engine. The optional runtime adds live frontier-model deliberation, private source retrieval, connectors and durable execution.

API: `GET /health`; `POST /api/v1/council`.

Every external action remains controlled by identity, policy, approval, proof and rollback.
