# Source coverage — v11.0.0

The canonical source is `GoalOS Scalable Autonomous Money Machine Ω` (`Pasted text(259).txt`). It contains **39 numbered records**. The release maps **39/39** records to public English and French routes in `SOURCE_COVERAGE_MATRIX.json`.

The machine-readable corpus is `data/canonical-corpus-v1100.json`. The French research interface provides source-grounded summaries while preserving the original English source excerpts as source records. Stable product names remain unchanged where they function as proper names.

Coverage does not convert a proposal, model, demonstration, illustrative financial scenario or publication into a claim of external validation, customer acceptance, live settlement or realized performance.
