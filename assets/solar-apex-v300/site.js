const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];const bar=q('.progress');addEventListener('scroll',()=>{const m=document.documentElement.scrollHeight-innerHeight;bar.style.width=(m?scrollY/m*100:0)+'%'});const mb=q('.menu'),mp=q('.mobile');if(mb)mb.onclick=()=>{const o=mp.classList.toggle('open');mb.setAttribute('aria-expanded',o)};qa('.mobile a').forEach(a=>a.onclick=()=>mp.classList.remove('open'));const steps=qa('.step'),panel=q('.panel');function pick(i){steps.forEach((x,j)=>{x.classList.toggle('active',i===j);x.setAttribute('aria-selected',i===j)});const x=steps[i].dataset;panel.innerHTML=`<div class="num">${x.num}</div><h3>${x.title}</h3><p>${x.body}</p><div class="rule">${x.rule}</div>`}steps.forEach((s,i)=>{s.onclick=()=>pick(i);s.onkeydown=e=>{if(['ArrowRight','ArrowDown','ArrowLeft','ArrowUp'].includes(e.key)){e.preventDefault();let n=i+(e.key==='ArrowRight'||e.key==='ArrowDown'?1:-1);n=(n+steps.length)%steps.length;steps[n].focus();pick(n)}}});pick(0);