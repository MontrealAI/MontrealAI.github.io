(()=>{const q=s=>document.querySelector(s);function sort(v){if(Array.isArray(v))return v.map(sort);if(v&&typeof v==='object')return Object.fromEntries(Object.keys(v).sort().map(k=>[k,sort(v[k])]));return v}async function digest(text){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(text));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}async function verify(){const out=q('#verify-result');try{const rec=JSON.parse(q('#record-text').value);const expected=rec.sha256;if(!/^[a-f0-9]{64}$/i.test(expected||''))throw new Error('The record does not contain a valid 64-character sha256 field.');delete rec.sha256;const canonical=JSON.stringify(sort(rec));const actual=await digest(canonical);const ok=actual.toLowerCase()===expected.toLowerCase();out.innerHTML=`<p class="${ok?'gold':''}"><strong>${ok?'DIGEST MATCH':'DIGEST MISMATCH'}</strong></p><p>Expected</p><p class="address">${expected}</p><p>Computed</p><p class="address">${actual}</p>`}catch(e){out.innerHTML=`<p><strong>Unable to verify.</strong></p><p>${String(e.message||e)}</p>`}}q('#verify-record')?.addEventListener('click',verify);q('#record-file')?.addEventListener('change',async e=>{const f=e.target.files?.[0];if(f)q('#record-text').value=await f.text()});q('#load-docket')?.addEventListener('click',async()=>{try{q('#record-text').value=await (await fetch('data/evidence-docket-000.json')).text();verify()}catch(e){q('#verify-result').textContent='Unable to load the bundled record.'}})})();